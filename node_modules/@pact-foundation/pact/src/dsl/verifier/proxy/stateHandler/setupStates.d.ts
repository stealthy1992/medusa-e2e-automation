import type { JsonMap } from '../../../../common/jsonTypes';
import type { ProviderState, ProxyOptions } from '../types';
export declare const setupStates: (state: ProviderState, config: ProxyOptions) => Promise<JsonMap | undefined>;
