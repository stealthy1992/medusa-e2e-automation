"use strict";
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.registerAfterHook = exports.registerBeforeHook = exports.registerHookStateTracking = void 0;
const logger_1 = __importDefault(require("../../../common/logger"));
const registerHookStateTracking = (hooksState) => async ({ body }, _res, next) => {
    if (body?.action === 'setup')
        hooksState.setupCounter += 1;
    if (body?.action === 'teardown')
        hooksState.setupCounter -= 1;
    logger_1.default.debug(`hooks state counter is ${hooksState.setupCounter} after receiving "${body?.action}" action`);
    next();
};
exports.registerHookStateTracking = registerHookStateTracking;
const registerBeforeHook = (beforeEach, hooksState) => async ({ body }, _res, next) => {
    if (body?.action === 'setup' && hooksState.setupCounter === 1) {
        logger_1.default.debug("executing 'beforeEach' hook");
        try {
            await beforeEach();
            next();
        }
        catch (e) {
            const error = e instanceof Error ? e : new Error(String(e));
            logger_1.default.error(`error executing 'beforeEach' hook: ${error.message}`);
            logger_1.default.debug(`Stack trace was: ${error.stack}`);
            next(new Error(`error executing 'beforeEach' hook: ${error.message}`));
        }
    }
    else {
        next();
    }
};
exports.registerBeforeHook = registerBeforeHook;
const registerAfterHook = (afterEach, hooksState) => async ({ body }, _res, next) => {
    if (body?.action === 'teardown' && hooksState.setupCounter === 0) {
        logger_1.default.debug("executing 'afterEach' hook");
        try {
            await afterEach();
            next();
        }
        catch (e) {
            const error = e instanceof Error ? e : new Error(String(e));
            logger_1.default.error(`error executing 'afterEach' hook: ${error.message}`);
            logger_1.default.debug(`Stack trace was: ${error.stack}`);
            next(new Error(`error executing 'afterEach' hook: ${error.message}`));
        }
    }
    else {
        next();
    }
};
exports.registerAfterHook = registerAfterHook;
//# sourceMappingURL=hooks.js.map